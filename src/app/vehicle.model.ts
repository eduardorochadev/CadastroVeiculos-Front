export interface Vehicle {
    id?: number; // O ID pode ser opcional ao criar um novo veículo
    placa: string;
    chassi: string;
    renavam: string;
    modelo: string;
    marca: string;
    ano: number;
  }